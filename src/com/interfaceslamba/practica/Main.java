package com.interfaceslamba.practica;

public class Main {
    public static void main(String[] args) {
        // Paso 1: Llamar al método "main" de la clase "RoboMailTest" para ejecutar el código relacionado con el test de Consumer.
        RoboMailTest.main(args);
        
        // Paso 2: Llamar al método "main" de la clase "ConsumerTest" para ejecutar el código relacionado con el test de Supplier.
        ConsumerTest.main(args);
        
        // Paso 3: Llamar al método "main" de la clase "FunctionTest" para ejecutar el código relacionado con el test de Function.
        FunctionTest.main(args);
        
        // Paso 4: Llamar al método "main" de la clase "BipredicateTest" para ejecutar el código relacionado con el test de Bipredicate.
        BipredicateTest.main(args);
        
        // Paso 5: Llamar al método "main" de la clase "SupplierTest" para ejecutar el código relacionado con el test de Supplier.
        SupplierTest.main(args);
    }
}

